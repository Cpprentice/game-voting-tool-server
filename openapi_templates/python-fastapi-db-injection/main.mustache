# coding: utf-8

{{>partial_header}}

from fastapi import FastAPI

{{#apiInfo}}
{{#apis}}
from {{apiPackage}}.{{classFilename}} import router as {{classname}}Router
{{/apis}}
{{/apiInfo}}
try:
    from {{ packageName }}.impl.lifespan import before_app_start
except ImportError:
    def before_app_start(x): return


app = FastAPI(
    title="{{appName}}",
    description="{{appDescription}}",
    version="{{appVersion}}",
)

{{#apiInfo}}
{{#apis}}
app.include_router({{classname}}Router)
{{/apis}}
{{/apiInfo}}

before_app_start(app)
